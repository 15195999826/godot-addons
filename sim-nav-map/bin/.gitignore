# committed: .dll / .wasm runtime artifacts (consumer projects need no toolchain)
# ignored: MSVC link intermediates
*.exp
*.lib
*.pdb
# Windows in-place-replace temp files (linker/editor overwriting a loaded dll)
~*.TMP
~*.dll
